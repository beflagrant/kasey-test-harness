<%= form_with(model: outtake, local: true) do |form| %>
  <% if outtake.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outtake.errors.count, "error") %> prohibited this outtake from being saved:</h2>

      <ul>
        <% outtake.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :full_name %>
    <%= form.text_field :full_name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :n_decimal %>
    <%= form.text_field :n_decimal %>
  </div>

  <div class="field">
    <%= form.label :n_float %>
    <%= form.text_field :n_float %>
  </div>

  <div class="field">
    <%= form.label :n_integer %>
    <%= form.number_field :n_integer %>
  </div>

  <div class="field">
    <%= form.label :is_ok %>
    <%= form.check_box :is_ok %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.date_select :birthday %>
  </div>

  <div class="field">
    <%= form.label :local_noon %>
    <%= form.time_select :local_noon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
